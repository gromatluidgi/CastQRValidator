<Page x:Class="CastQRValidator.Views.Pages.SamplesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CastQRValidator.Views.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="SamplesPage">

    <DockPanel Margin="16" LastChildFill="True">
        <StackPanel
            HorizontalAlignment="Left"
            DockPanel.Dock="Top"
            Orientation="Vertical">
            <TextBlock Style="{StaticResource MaterialDesignHeadline3TextBlock}" Text="Samples" />
            <TextBlock Style="{StaticResource MaterialDesignSubtitle1TextBlock}" Text="Performs read, create, update, delete operations over the samples datasource." />
        </StackPanel>
        <DataGrid
            Margin="0,16,0,0"
            AutoGenerateColumns="True"
            CanUserAddRows="False"
            HeadersVisibility="All"
            ItemsSource="{Binding Samples}" />
    </DockPanel>
</Page>
